{{#if useProps}}
import React from 'react'
import { Text } from 'react-native'
import { {{pascalCase name}}ScreenProps } from './{{pascalCase name}}.types'

export const {{pascalCase name}}Screen = ({ }: Readonly<{{pascalCase name}}ScreenProps>) => {
    return (
        <S.{{pascalCase name}}>
            <Text accessibilityRole="text">{{pascalCase name}}</Text>
        </S.{{pascalCase name}}>
    )
}


{{else}}
import React from 'react'
import { Text } from 'react-native'

export const {{pascalCase name}}Screen = () => {
    return (
        <S.{{pascalCase name}}>
            <Text accessibilityRole="text">{{pascalCase name}}</Text>
        </S.{{pascalCase name}}>
    )
}


{{/if}}
